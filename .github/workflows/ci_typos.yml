name: Typos
on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

jobs:
  typos:
    name: Spell Check
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: crate-ci/typos@v1
